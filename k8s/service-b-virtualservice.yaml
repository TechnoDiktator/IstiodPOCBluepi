apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: service-b
  namespace: default
spec:
  hosts:
  - "*"
  gateways:
  - istio-system/service-a-gateway
  http:
  - match:
    - uri:
        prefix: /products  # Example API
    route:
    - destination:
        host: service-b.default.svc.cluster.local
        port:
          number: 8081


# all the virtualservices have the same gateway ????? 
