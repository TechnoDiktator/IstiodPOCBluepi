apiVersion: v1
kind: Service
metadata:
  name: service-c
spec:
  selector:
    app: service-c
  ports:
  - protocol: TCP
    port: 8083
    targetPort: 8083
  clusterIP: None  # Makes Service C only accessible inside the cluster
