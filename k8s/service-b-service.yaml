apiVersion: v1
kind: Service
metadata:
  name: service-b
spec:
  selector:
    app: service-b
  ports:
  - protocol: TCP
    port: 8081
    targetPort: 8081
  clusterIP: None  # Makes Service B only available inside the cluster
