apiVersion: v1
kind: Service
metadata:
  name: istio-ingressgateway
  namespace: istio-system
spec:
  type: NodePort  # 👈 Exposes Istio Gateway externally on a fixed port
  selector:
    istio: ingressgateway
  ports:
  - name: http
    protocol: TCP
    port: 80       # 👈 Inside the cluster, services access the Gateway at port 80
    targetPort: 8080  # 👈 Inside the Pod, Istio listens on port 8080
    nodePort: 31380  # 👈 Exposes the service externally on the Kubernetes node at port 31380
